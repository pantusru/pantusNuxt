export interface interfaceFormSearch {
  minPrice: number;
  maxPrice: number;
  checkedBrand: number[];
  sortType: string;
  sortName: string;
  search: string;
  checkedApplicability: number[];
  checkedCategories: number[];
}
